import Header from "@/components/Header";
import ListItem from "@/components/ListItem";

export default function Home() {
  return (
    <div className="
      bg-neutral-300
      rounded-lg
      h-full
      w-full
      overflow-hidden
      overflow-y-auto
    ">
      {/* // homepage vera e propria (quella che hai mostrato in foto), con i contenuti dinamici */}
    </div>
  );
}
